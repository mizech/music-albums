<h2>Artists</h2>
<% @artists.each do |artist| %>
    <div>
        <h3><%= artist.name %></h3>
        <p>Years active: <%= artist.years_active %></p>
        <p>Members: <%= artist.members %></p>
    </div>
<% end %>
